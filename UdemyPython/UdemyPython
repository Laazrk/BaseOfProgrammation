#Days 9 : learning using dictionnary and nesting in Python
#student_informations ={
#    "Nombre":"29",
#    "Noms": ["Carolina", "Tsiory", "Laza"]  
#}

#import logo from art.py
from art import logo_bids

#print the logo
print(logo_bids)
#create the global dictionnary
primary_Dictionnary={}

#loop to not quit the application
letsgo='y'
while letsgo == 'y' :
    #ask the name 
    name = input('what is your name? :')

    #ask for the bids
    primary_Dictionnary[name]=input('what is your bids? :')

    #TBC : To be confirmed/
    #ask the name 
    name = input('what is your name? :')

    #ask for the bids
    primary_Dictionnary[name]=input('what is your bids? :')

    #ask if you want to continue 
    letsgo = input('Do you want add new player ? [Y/n]')

#the function max have to find the int value max in dictionnary
keyElement=max(primary_Dictionnary, key=primary_Dictionnary.get)

#Afficher le vainqueur
print("the personn who wins the bild is", keyElement)

#print an element in a dictionnary (nesting)
#print(student_informations["Noms"][0])

#Days 10 for learning Python
#fonction with output
#def format_name(f_name, l_name):
#    print(f_name.title())
#    print(l_name.title())

#format_name("john","doe")
